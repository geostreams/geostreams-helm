apiVersion: v1
kind: Service
metadata:
  name: {{ include "geostreams.fullname" . }}
  labels:
{{- include "geostreams.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geostreams.selectorLabels" . | nindent 4 }}
